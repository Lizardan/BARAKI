# Local playtest secrets - copy from playtest.env.example and fill in.
# Do not commit this file.
MATCHMAKER_URL=https://baraki-matchmaker.lizard268.workers.dev
REGISTER_SECRET=replace-me
# Optional named tunnel. Prefer empty + ALLOW_QUICK_TUNNEL=1:
# Discord Portal /wss → matchmaker hostname once (Worker proxies to tonight's tunnel).
# Example Portal target: baraki-matchmaker.YOURACCOUNT.workers.dev
# Named tunnel (needs Cloudflare zone/domain): .\infra\scripts\setup-named-tunnel.ps1
WSS_HOST=
TUNNEL_NAME=baraki-game
# 1 = quick tunnel each evening (hostname changes in KV only; Discord /wss stays on Worker).
ALLOW_QUICK_TUNNEL=1
# Optional absolute path to cloudflared.exe if not on PATH:
# CLOUDFLARED_EXE=C:/Tools/cloudflared/cloudflared.exe
# Absolute path to dedicated headless build:
SERVER_EXE=F:/Unity Projects/BARAKI/Builds/WindowsServer/BARAKI.exe
PORT=7777
PLAYERS=2
