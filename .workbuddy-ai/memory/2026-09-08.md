# 2026-09-08

## Доступ к задачам HacknPlan
- Пользователь ведёт задачи в расширении UnioTasks (источник — HacknPlan проект 242091).
- Агенту UnioTasks UI недоступен; задачи читаются напрямую через API: `Tooling/HacknPlan/client.js`
  (`HacknPlanClient.fromEnv()`, ключ в `Tooling/HacknPlan/.secrets.json`).
- Создан read-only скрипт `Tooling/HacknPlan/list-open.js` (группы как в панели UnioTasks,
  флаги `--all` / `--json`).
- Подтверждено: API v0 отдаёт stage/importanceLevel/category/board вложенными объектами,
  плоских id нет. Записано в `wiki/rules/github-issues-workflow.md`.
- Снимок backlog: 68 карточек, 14 открытых (все stage Planned, In progress / Testing пусты),
  54 Completed.

## FACELESS-008 (дизайн asymmetry kit Древних) — #63
- Карточка #63: Planned → In progress → **Testing** (итог записан комментарием).
- Спроектировано и записано в `GameDesign/Races.md`:
  - пассивы: `PASSIVE_FACELESS_ABYSSAL_HUNGER` (вампиризм 10% юнитам),
    `PASSIVE_FACELESS_RELENTLESS_TIDE` (+10% AS юнитам),
    `PASSIVE_FACELESS_BLEAK_FOUNDATIONS` (−10% max HP зданиям);
  - магия ×3: `SPELL_FACELESS_1..3` (Гниющий взор / Вытягивание жизни / Поднять павшего);
  - tower-треки ×9 `UPG_TOWER_FACELESS_*` (Ravenous Strikes … Devour), только юниты, L1–L3;
  - уники 11–12 = Shadow of the Void / Void Bastion (из FACELESS-010).
- Идентичность расы: Люди = сырые статы и оборона; Древние = темп и самоппитка
  ценой хрупкой базы. `SelectableRaceIds` не менялся (unlock после GATE).
- Созданы follow-up реализации: #71 FACELESS-015, #72 FACELESS-016, #73 FACELESS-017
  (скрипт `Tooling/HacknPlan/create-faceless-kit-followups.js`).
- Зависимости карточек через API ставить нельзя (403) — только текстом.
- Пользователь потребовал: tower-треки не должны дублировать механики бонусов (FACELESS-010).
  Аудит: 7 из 9 пересекаются (#1 вампиризм, #2 дот on-hit, #4 призыв, #5 промах — явные;
  #3 AoE при смерти, #7 on-kill баф, #9 лечение за убийство — «чуть другие»).
  Оставлены #6 (плоский AS) и #8 (скорость). Пользователю выдано по 3 варианта замены на
  каждую позицию — ждём выбора, правки Races.md после него.
- Свободные оси для замен (не заняты ни Faceless-бонусами, ни Human-треками): пробитие брони,
  крит, снижение входящего урона, max HP, урон по зданиям, сплеш, дебаф при смерти,
  золото за убийство (новый хук), замедление/метка (новые механики).
- Пользователь выбрал: 1B, 2A, 3B, 4C, 5A, 7C, 9A → треки Chitinous Hide / Hollow Barbs /
  Vacuum Collapse / Ritual of the Deep / Unnerving Aim / Splash of the Deep / Void Hardening
  (#6 Frenzy of the Deep и #8 Hollow Bones оставлены). L3 #1 заменён на «+15% max HP при
  спавне» — иначе дублировал #9 (срез урона от зданий).
- Правило «трек башни не дублирует механику бонуса» + список запрещённых/свободных осей
  записано в `wiki/rules/tower-tracks.md` (раздел «Как добавить трек новой расе»).
