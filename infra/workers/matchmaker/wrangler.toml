name = "baraki-matchmaker"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Set once after first deploy:
#   npx wrangler secret put REGISTER_SECRET
#
# FREE-0 uses in-memory match/tunnel state (no KV required for CI).
